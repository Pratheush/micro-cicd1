# WE MAINLY NEED PROMETHEUS SPECIFIC METRICS OF OUR APPLICATION SO WE EXPOSE THAT
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: task-app
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://172.26.238.72:5432/task_service_db
    username: task_service_user
    password: taskpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 9393
